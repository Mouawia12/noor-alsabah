/vendor
/node_modules
/public/uploads
.env

# secrets & logs (أُزيلت — كانت مكشوفة)
/env
error_log
*.log
/storage/*.key
/public/storage
/public/build

# وثائق العميل الداخلية (لا تُرفع — المستودع عام)
CLIENT_REQUIREMENTS_CHECKLIST_AR.md

# database dumps / backups (لا تُرفع أبداً — قد تحتوي بيانات وكلمات مرور)
*.sql
*.sql.gz
*.dump
*.bak
/database/dumps
/database/backups
