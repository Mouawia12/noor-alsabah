# جدول تقدّم المشروع — الذكاء الاصطناعي للمشتريات والإيجارات

> يُحدَّث مع كل إنجاز. ✅ = منجز ومُختبَر | 🔄 = جارٍ | ⬜ = لم يبدأ | 👤 = على المالك/الخادم

**آخر تحديث:** 2026-07-02

---

## نظرة عامة على التقدّم

| المرحلة | النسبة | الحالة |
|---------|:------:|:------:|
| 0. التهيئة والبنية التحتية | 100% | ✅ |
| 1. أساس الباك-اند (طبقة المحرك) | 100% | ✅ |
| 2. وحدة المشتريات | 100% | ✅ |
| 3. وحدة الإيجارات | 100% | ✅ |
| 4. التنبيهات والبريد | 100% | ✅ |
| 5. التحليلات والتنبؤات | 100% | ✅ |
| 6. التحسينات المتقدمة | 100% | ✅ |
| 7. الاختبارات الآلية | 100% | ✅ |
| **النطاق الأصلي (المراحل 0–7)** | **100%** | ✅ |
| 8. النشر والتشغيل | 0% | 👤 على المالك |
| **9. ملاحظات وطلبات العميل الجديدة (28 يونيو)** | **0%** | 🔄 قيد العمل |

```
النطاق الأصلي:      ██████████████████████████████  100% ✅
طلبات العميل الجديدة: ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░    0%  🔄
النشر:              ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░    0%  👤
```

---

## التفصيل بعلامات الإنجاز

### المرحلة 0 — التهيئة (100%) ✅
- [x] حزم معالجة PDF (fpdi, pdfparser)
- [x] `config/ai.php` + مفاتيح `.env`
- [x] الطابور `database` + هجرتا jobs/notifications
- [x] قرص تخزين خاص `ai_private`
- [x] هجرات بنية البيانات (supplier, import batch/item, audit_log...) + تشغيلها

### المرحلة 1 — أساس الباك-اند (100%) ✅
- [x] واجهة `ExtractionEngine` المعزولة
- [x] محوّل `OpenAiEngine` (رؤية + Structured Outputs + إعادة محاولة)
- [x] `ExtractionManager` + `ExtractionResult`
- [x] مخططات الفاتورة والعقد (strict + ثقة لكل حقل)
- [x] النماذج (Supplier, Import Batch/Item, AuditLog)

### المرحلة 2 — المشتريات (100%) ✅
- [x] رفع الفواتير (PDF/صور) + منع تكرار الملف
- [x] معالجة خلفية: تحويل لصور + تجزئة بكشف حدود ذكي
- [x] استخراج + تصعيد عند ضعف الثقة + تحقق رياضي
- [x] كشف الفواتير المكررة + مطابقة الموردين
- [x] شاشة المراجعة والاعتماد (+ صورة المستند + إبراز الثقة)
- [x] شاشة الفواتير الفاشلة + إعادة المعالجة
- [x] تقارير وإحصائيات

### المرحلة 3 — الإيجارات (100%) ✅
- [x] رفع عقود الإيجار + معالجة
- [x] استخراج بيانات العقد
- [x] مراجعة واعتماد + اختيار المحل
- [x] **توليد جدول الدفعات تلقائياً**
- [x] شاشة العقود الفاشلة + إعادة المعالجة
- [x] تقارير الإيجارات (نشطة/منتهية/دفعات)

### المرحلة 4 — التنبيهات والبريد (100%) ✅
- [x] تنبيهات دفعات مستحقة/متأخرة + عقود قاربت الانتهاء
- [x] إشعارات داخل النظام + بريد إلكتروني
- [x] أمر مجدول يومي `rent:alerts`
- [x] لوحة متابعة الإيجارات + تعليم الدفع
- [x] صفحة الإشعارات + عدّاد غير المقروء
- [x] **جرس إشعارات احترافي في الترويسة**: أيقونة عاملة + قائمة منسدلة بأحدث الإشعارات + عدّاد حيّ (استطلاع دوري) + تعليم فردي/جماعي كمقروء عبر AJAX + عرض موحّد لكل الأنواع (`NotificationPresenter`، `partials/topbar/_notifications-bell`، `NotificationController::recent`). مغطّى باختبار `NotificationSystemTest`.
- [x] إشعار اكتمال معالجة الدفعة

### المرحلة 5 — التحليلات والتنبؤات (100%) ✅
- [x] التدفق النقدي المتوقّع (12 شهراً)
- [x] الإيرادات المتوقّعة + نسبة التحصيل
- [x] المحلات عالية المخاطر
- [x] اتجاه المشتريات
- [x] ملخّص ذكي بالـAI + رسوم بيانية تفاعلية

### المرحلة 6 — التحسينات المتقدمة (100%) ✅
- [x] عرض صورة المستند الأصلي في المراجعة
- [x] إبراز الحقول منخفضة الثقة
- [x] زر إعادة الاستخراج
- [x] **التعلّم المستمر** من تصحيحات المستخدم
- [x] رسوم بيانية تفاعلية (ApexCharts)
- [x] إصلاح المسارات المكرّرة (route:cache يعمل)
- [x] أداة تنظيف الدفعات القديمة

### المرحلة 7 — الاختبارات الآلية ✅
اختبارات حقيقية تعمل على sqlite في الذاكرة (بدون Oracle/OpenAI). `php artisan test` → أخضر (93 اختباراً).
- [x] توليد جدول الدفعات: شهري + تواريخ صريحة + عدم تجاوز نهاية الشهر + تسوية باقي التقريب (`tests/Unit/Ai/PaymentScheduleServiceTest`)
- [x] تطبيع التواريخ (DD/MM، أرقام عربية، رفض القيم غير الصالحة) (`tests/Unit/Ai/DateNormalizerTest`)
- [x] التحقق المنطقي من الفاتورة (الرياضيات + الحقول) (`tests/Unit/Ai/InvoiceValidationServiceTest`)
- [x] المعالجات الأمنية (اعتماد Oracle من env، حماية `/clear` والرفع، تعطيل التسجيل الذاتي) (`tests/Feature/SecurityHardeningTest`, `RegistrationTest`)
- [x] مصادقة المستخدمين (تسجيل الدخول/الخروج/كلمة المرور)
- [x] **تغطية المسار الكامل للمشتريات والإيجارات بمحرك استخراج وهمي** (بلا OpenAI/PDF حقيقيين): تجزئة الصفحات → استخراج → قبول/رفض → عدّادات الدفعة واكتمالها → اعتماد وإنشاء سجل المشتريات؛ وللإيجارات: كشف تكرار العقد + توليد جدول الدفعات آلياً. (`tests/Feature/PurchasePipelineTest`, `RentPipelineTest`, `tests/Support/Fake*`)

### المرحلة 8 — النشر والتشغيل (0%) 👤 على المالك
- [ ] تثبيت poppler-utils + ghostscript على الخادم
- [ ] تشغيل الطابور عبر supervisor
- [ ] cron للمجدول (`schedule:run`)
- [ ] ضبط `.env` على الخادم (مفتاح + بريد + DB)
- [ ] التجربة بفواتير/عقود حقيقية
- [ ] **تدوير كلمة مرور Oracle المكشوفة** — نُقلت من الكود إلى `.env`، لكنها ما زالت في تاريخ git (أول commit). يجب تغييرها على الخادم فعلياً + ضبط `.env`.

### المرحلة 9 — ملاحظات وطلبات العميل الجديدة (28 يونيو 2026) 🔄
> طلبات جديدة من العميل بعد التجربة الأولى. الهدف: نظام أقوى وأسرع واحترافي.

**أ) تطوير محتوى إشعارات الإيجارات** ✅ (بريد + داخل النظام + لوحة المتابعة)
- [x] رقم العقد
- [x] اسم المؤجر
- [x] اسم المستأجر
- [x] اسم العقار/الوحدة المؤجرة
- [x] رقم الدفعة (الدفعة 3 من 12)
- [x] قيمة الدفعة
- [x] تاريخ الاستحقاق
- [x] حالة الدفعة (متأخرة / مستحقة قريباً)
- [x] عدد أيام التأخير للدفعات المتأخرة

**ب) مستند رسمي لتأكيد استلام الدفعات** ✅
- [x] إصدار سند استلام رسمي (PDF بترويسة المنشأة + الشعار) عند تأكيد استلام دفعة
- [x] تضمين اسم الموظف المستلِم + خانات التوقيع + المبلغ رقماً وكتابةً
- [x] **ترويسة السند قابلة للتخصيص** (مطبوعة الفوالين): إعداد `ai.receipt_letterhead` (`RECEIPT_LETTERHEAD` في `.env`) — يكفي أن يرفع العميل ملف الترويسة ويضبط المسار، فتُستخدم كترويسة كاملة العرض بلا تعديل كود ولا نشر جديد. (`config/ai.php`, `RentAlertsController::receipt`)

**ج) تحسين تجربة معالجة الفواتير** (رفع 10 فواتير وقُبلت 3 دون معرفة المرفوضة)
- [x] رسالة/شريط تقدّم حيّ أثناء المعالجة + **رسالة طمأنة صريحة** «جاري معالجة الفواتير الآن، يرجى الانتظار...» (صفحة المتابعة)
- [x] فرز واضح بعد المعالجة: **مقبولة** (المراجعة) مقابل **مرفوضة** (شاشة مستقلة)
- [x] لكل فاتورة مرفوضة **سبب واضح** (رقم الفاتورة غير مقروء / الإجمالي غير مقروء / صورة غير واضحة / الملف لا يحتوي فاتورة)
- [x] شاشة مستقلة للفواتير المرفوضة + إعادة المعالجة
- [x] **ترحيل الفواتير المقبولة إلى فرع/محل**: قائمة منسدلة بكل المحلات + الفرع إلزامي قبل الترحيل + زر «ترحيل الفواتير» (فردي وجماعي) + ربط `shop_id` فعلياً بسجل المشتريات. (`PurchaseImportService::approveItem`, `PurchaseAiController::approve/approveAll`, `review.blade.php`) — مغطّى باختبار `PurchasePipelineTest`.
- [x] إنشاء سجلات مشتريات مربوطة بالفرع + حفظ نسخة الفاتورة + رسالة تأكيد «تم ترحيل الفواتير المقبولة إلى الفرع المحدد بنجاح» (توستر)
- [x] **إظهار اسم الفرع بوضوح** (طلب العميل: «لا يظهر اسم الفرع الذي تُرحَّل إليه»): مؤشّر حيّ «الفرع المحدد: X» + اسم الفرع في نافذة التأكيد وفي رسالة النجاح (فردي وجماعي) + التحقق أن الفرع موجود قبل الترحيل. مغطّى باختبار `PurchasePipelineTest`.
- [x] عدّادات نتائج واضحة بعد الرفع: بطاقات ملوّنة (إجمالي/مقبولة/مرفوضة) + ملخّص اكتمال + أزرار بعدّاد لكل فئة (مشتريات وإيجارات).

**د) تقوية وصلابة النظام** ✅
- [x] تحصين أمني + تطويق دوال FPM + تأمين الإشعارات (جلسة المراجعة)
- [x] عدّادات الدفعة من قاعدة البيانات (مقاومة لإعادة المحاولة، لا ازدواج عدّ)
- [x] معالجة رفض النموذج/الاقتطاع + رسائل فشل ودّية بدل التقنية
- [x] **صلابة معالجة الملفات الكبيرة/كثرة الفواتير**: (1) حدّ أقصى `max_pages_per_batch` يُفشل الملف الضخم برسالة واضحة قبل إرهاق الخادم؛ (2) `ImageOptimizer` يُصغّر ويضغط الصور الكبيرة (صور الجوال) قبل إرسالها لـOpenAI — رفع أسرع وذاكرة أقل ومهلة أأمن، مع تجاوز صفحات PDF الصغيرة دون أثر عكسي. مغطّى بـ`ImageOptimizerTest` و`PurchasePipelineTest`.
- [x] **رفع بـAJAX** مع شريط تقدّم + حماية `beforeunload` من فقدان الرفع + معالجة خلفية (مغطّى بـ`PurchaseUploadTest`).
- [x] مجموعة اختبارات آلية خضراء (93 اختباراً)

**هـ) ترقية نموذج الذكاء الاصطناعي للأقوى**
- [x] الترقية إلى **`gpt-5.5`** (أحدث/أقوى نموذج رؤية chat متاح للمفتاح) + جعل temperature اختيارياً
- [x] **قرار: الإبقاء على `gpt-5.5` بلا بديل أسرع/أضعف** — الأولوية للدقة والنتائج القوية، ومنطق التصعيد يرفع للأقوى عند ضعف الثقة. أي «بديل أسرع» يعني نموذجاً أضعف فلا نضيفه.
- [ ] (بعد تشغيل الخادم) قياس الدقة/السرعة على فواتير حقيقية بالنموذج الجديد — لم يُجرَّب `gpt-5.5` فعلياً بعد لأن الخادم ما زال على `gpt-4o-mini`.

**و) دقة استخراج الفواتير وتقاريرها (ملاحظات 30 يونيو – 1 يوليو 2026)** ✅
> ملاحظات العميل بعد التجربة: رفع 10 فواتير وقُبلت 3، وضعف في قراءة الرقم الضريبي.
- [x] **تحسين استخراج الرقم الضريبي للمورد** — تعليمات صريحة في المطالبة (15 خانة سعودي، عناوين VAT/TRN/الرقم الضريبي، وعدم الخلط مع السجل التجاري/رقم الفاتورة). `InvoiceSchema::prompt()`
- [x] **نقل اسم الشركة/المورد حرفياً** كما يظهر (عربي/إنجليزي) دون ترجمة — تعليمات صريحة في المطالبة.
- [x] **تقرير حالة كل فاتورة** لكل ملف: نجح/فشل/السبب + الحقول الأساسية + الثقة، مع تصدير PDF/Excel. `PurchaseAiController::batchReport` + `batch_report.blade.php` (مسار `purchase.ai.batch.report`).
- [x] **منع تكرار الفاتورة عند الحفظ**: `PurchaseImportService::approveItem` يرفض رقم فاتورة موجوداً برمي `DuplicateInvoiceException` ورسالة «فاتورة مكررة» في الواجهة (اعتماد فردي + جماعي). مغطّى باختبار.
- [x] التحقق من صحّة **الرقم الضريبي** (10–15 خانة) دون رفض الأرقام السليمة — `InvoiceValidationService`.
- [x] **أسباب التحذير التي ذكرها العميل صراحةً**: «الرقم الضريبي غير موجود» (تمييز الغياب الكامل عن الصيغة الخاطئة) + «قيمة الضريبة غير متطابقة» — تُعرض كتحذيرات واضحة على الفاتورة المقبولة (لا رفض صارم لتفادي مشكلة «قُبلت 3 من 10»). مغطّى باختبارات `InvoiceValidationServiceTest`.
- [ ] (متابعة) قياس دقة الاستخراج على فواتير حقيقية بعد تشغيل الخادم بالنموذج الجديد.

**ح) لمسات احترافية وأداء (إبداع إضافي)** ✅
- [x] **لوحة معلومات ذكاء اصطناعي جديدة** (`/dashboard/ai/overview`): ترويسة ترحيبية + بطاقات KPI حيّة (دفعات/فواتير معالَجة/مُرحّلة/بانتظار/مرفوضة/إجمالي المشتريات) + حلقة دقّة (Donut بـCSS خالص بلا CDN) + ملخّص الإيجارات + خطّ زمني للنشاط الأخير + أزرار وصول سريع. (`AiDashboardController`, `AiDashboardStats`, `overview.blade.php`)
- [x] **تخزين مؤقت (Cache)** لمؤشّرات اللوحة (60ث) مع إبطال فوري عند الترحيل/الرفض (`AiDashboardStats::forget`) — تخفيف الحِمل على القاعدة.
- [x] **فهارس أداء مركّبة** لتسريع قوائم المراجعة والنشاط: `(status, created_at)` على عناصر المشتريات والإيجارات + فهرس `created_at` على سجل التدقيق. (هجرة `add_ai_performance_indexes`)
- [x] رابط «لوحة الذكاء الاصطناعي» في القائمة الجانبية. مغطّى باختبار `AiDashboardStatsTest`.

**ز) تشغيل فعلي على الخادم (سبب «لا أرى تغييراً»)** ⬜
> العميل يرى النظام «كما هو» رغم رفع التحديثات — غالباً فجوة نشر/تشغيل لا كود.
- [ ] نشر آخر إصدار على `noor-alsabah.souftech.com` (git pull + مسح الكاش على الخادم).
- [ ] تشغيل عامل الطابور `php artisan queue:work` (بدونه لا تُعالَج الفواتير/العقود ولا تُرسل الإشعارات).
- [ ] cron للمجدول `schedule:run` (تنبيهات الإيجارات) + تثبيت poppler/ghostscript.
- [ ] ضبط `OPENAI_API_KEY` والنموذج على الخادم والتحقق أن `gpt-5.5` متاح للمفتاح فعلاً.

**ط) ملاحظات شاشة «مصاريف شراء المحلات» (2 يوليو 2026)** ✅
> ملاحظات العميل على وحدة مصاريف شراء المحلات للفرع (الوحدة القائمة `PurchaseController`/`purchase`).
- [x] **منع ترحيل/إدخال فاتورة مكررة** — المعيار **رقم الفاتورة**: `unique:purchase` في الإدخال (`store`) والتعديل (`updstore`) مع رسالة صريحة «فاتورة مكررة»، وفي ترحيل الـAI عبر `DuplicateInvoiceException`.
- [x] **مدخِل الفاتورة تلقائياً** = المستخدم الحالي (`create_user => Auth::user()->id`) دون إدخال يدوي — كان منفّذاً ومؤكَّداً.
- [x] **أكواد الفروع** — عمود `shop_code` (هجرة) + حقل في نموذج المحل (إضافة/تعديل) + بحث بالكود + عرضه بجانب الاسم في قوائم الاختيار (شاشة الشراء وترحيل الـAI للمشتريات والإيجارات).
- [x] **مطابقة تصدير Excel للشاشة** — أُعيد بناء `print_purchase_xlsx` بـ12 عموداً مطابقة لجدول `tbl_purchase` بنفس الترتيب (أُضيفت: الضريبة، المبلغ دون الضريبة، الرقم الضريبي، المدخِل).
- [x] **تصدير PDF بالتنسيق الرسمي** — ترويسة نور الصباح الرسمية (شعار أعلى الصفحة + تذييل: العنوان/البريد/س.ت 2055128621) في `print_purchase_pdf` باستخدام `public/assets/media/reports/`.

**ي) إصلاحات ترحيل الفواتير + توسعة الأكواد (5 يوليو 2026)** 🔄
> بعد تجربة العميل على الخادم.
- [x] **اسم المورد لا يظهر بعد الترحيل** — `approveItem` كان يحفظ `supplier_id` فقط؛ الآن يحفظ أيضاً `purchase_respon` (الاسم الذي تعرضه الشاشة). مغطّى باختبار.
- [x] **شارة «فاتورة مكرّرة»** في شاشة المراجعة قبل الترحيل — تكشف المكرر الموجود مسبقاً في المشتريات + **المكرر داخل نفس الملف** (فاتورتان بنفس الرقم).
- [x] **بحث بالكتابة (بالاسم أو الكود)** في قائمة اختيار الفرع بشاشة الترحيل (Select2).
- [x] عرض **كود الفرع** في عمود «المحل» بشاشة الشراء وتصدير Excel.
- [x] **شاشة «إدارة أكواد المحلات» المستقلة** (`/dashboard/shop-codes`): قائمة (كود/اسم/حالة فعّال-غير فعّال/تاريخ الإنشاء/المنشئ) + **تحقّق فريد** للكود + تعيين/تعديل + تفعيل/إلغاء + بحث بالاسم أو الكود. `ShopCodeController` + هجرة `shop_code_active/by/at` + رابط في القائمة الجانبية. مغطّاة بـ5 اختبارات (`ShopCodeTest`).
- [ ] أداء شاشة الشراء (تقليل حد العرض الافتراضي من 3000) — جذره ضعف الخادم (يُعالَج تشغيلياً).
> تنبيه: نص «نسخة مكررة…» الذي رآه العميل في عمود الملاحظة هو **نصّ الفاتورة التجريبية نفسها** (استخرجه الذكاء كملاحظة)، وليس رسالة نظام.

---

## خارج النطاق (مؤكّد)
- ربط الزكاة / ZATCA (العميل: نشتغل على الأساسي).
